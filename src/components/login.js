import { Link } from "react-router-dom";

export const Login = ({setLogin, setPage}) => {
    return (
        <div>
            <header>
                <h1>Social Sushi</h1>
            <header/> 
        </div>
    )
}